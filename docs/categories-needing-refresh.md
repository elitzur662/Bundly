# Categories needing refresh / scraping

Living document — list of category-tree leaves whose ZAP_SOG_MAP entry is
valid but whose cache is either empty or yields zero usable products under
the front-end Quality Gate. Each row shows the leaf name, the target ZAP
sog, and the recommended action.

Generated by `audit-categories.mjs` after the 2026-05-15 tree pruning.
Re-run the script after each pre-warm pass to see what's still pending.

## Broken — cache populated but no rows pass the Quality Gate

These had cached candidates without prices/images. Likely a stale crawl;
re-running the per-model price fetch should fix them.

| Leaf | Sog | Symptom |
|---|---|---|
| קולנוע ביתי | `e-hometheater` | 35 candidates, 0 with prices |
| חצובות | `h-tripod` | 168 candidates, 7 brand-only, 0 priced |
| תיקי מצלמה | `h-camerabag` | 834 candidates, 373 brand-only |
| מחשבים נייחים | `c-pcdesktop` | 151 candidates, 0 priced — sog is real but per-model fetch failing |
| מחשבי All-in-One | `c-pcdesktop` | same — same sog |
| iMac | `c-pcdesktop` | same |
| מחשבי גיימינג | `c-pcdesktop` | same |
| מייבשי שיער | `e-hairdrayer` | 108 candidates, 0 priced |
| אליפטיקל | `s-crosstrainer` | 126 candidates, 77 brand-only |
| מכשירי חתירה | `s-crosstrainer` | same |

**Action:** force a price re-crawl for these sogs. Likely candidates ran
during a ZAP CF-block window. Try `npm run sync-and-export` after a CF
cool-off.

## No-cache — sog mapped but never warmed

These were either added to `ZAP_SOG_MAP` recently and haven't been picked
up by the prewarm queue, or got skipped due to CF blocking. Should populate
on the next category pre-warm cycle (every 6h).

### Kitchen extras
- טוסטרים (`e-toster`)
- מיקסרים (`e-mixer`)
- מעבדי מזון (`e-foodproccessor`)
- מסחטות (`e-squeezer`)
- סירי בישול וטיגון (`h-cookingpots`)
- פלטות חשמליות (`e-plata`)
- מכונות שטיפה וטאטוא (`h-washer`)

### AV
- מיקרופונים (`e-microphone`)
- מציאות מדומה (`e-vrglasses`)

### Photo / surveillance
- מצלמות אבטחה (`g-hiddencam`)

### Phones / wearables
- שעונים חכמים (`e-cellwatch`)
- אביזרי סלולר (`e-cellphonecase`)
- מטענים (`e-charger`)

### PC hardware
- מעבדים (`c-cpu`)
- לוחות אם (`c-motherboard`)
- זיכרון RAM (`c-memory`)
- כוננים SSD (`c-harddrive`)
- ספקי כוח (no sog — needs mapping)
- מארזי מחשב (`c-tower`)
- מאווררים וקירור (`c-fan`)
- שרתים (`c-server`)
- מחשבי מיני (`c-brandpc`)
- Mac Mini (`c-brandpc`)

### Peripherals
- סורקים (`c-scanner`)
- רמקולים למחשב (`c-speakers`)
- שולחנות גיימינג (`c-gamingtable`)

### Network / storage
- ראוטרים (`c-router`)
- מגדילי טווח WiFi (`c-repeater`)
- מתגי רשת (`c-hub`)
- כוננים קשיחים (`c-harddrive`)
- זיכרונות USB (`c-diskonkey`)
- כרטיסי זיכרון (`c-flashmemory`)
- NAS שרתי אחסון (`c-nasserver`)
- כוננים חיצוניים (`c-harddrive`)

### Beauty (all currently no-cache, mainstream consumer demand)
- כל "הסרת שיער" (`e-hairremover` × 5)
- כל "גילוח" (`e-shaver` × 3 + `e-ladyshaver` × 1)
- כל "טיפוח פנים" (`e-beautymachine` × 6)
- כל "עיסוי ורלקסציה" (`e-massager` × 4 + `e-massage` × 1)

### Sport / fitness
- אופניים נייחים חשמליים (`s-exercisebike`)

### Mobility
- קורקינטים חשמליים (`s-electricscooter`)
- קלנועיות (`s-electricscooter`)
- מונופד חשמלי (no good sog)
- Hoverboard (`s-electricscooter`)
- סגוויי (`s-electricscooter`)

### Health
- מדי לחץ דם (`e-bloodpressure`)
- מד חמצן (`b-bloodpressure`)
- נבולייזרים (`e-nebulizer`)
- מכשירי TENS (`e-tens`)
- מד חום חשמלי (`e-thermometer`)
- מכשירי EMS (`s-abs`)
- שמיכות חשמליות (`e-electricblanket`)

### Tools
- מברגות חשמליות, מקדחות חשמליות, מסורי דיסק, מסורי ג'יגסאו, מטחנות זווית,
  מכשירי שיוף, נעצות חשמליות, מפוחים חשמליים (all `b-powertools`)

### Smart home
- נורות LED חכמות, שקעים חכמים, פעמוני דלת חכמים, בקרי תאורה חכמים,
  מנעולים חכמים, חיישני תנועה (all `b-smarthome`)

**Action:** run a category pre-warm pass. The prewarm queue iterates sogs
on a cron — wait one cycle (≤6h post-deploy) and re-audit. Anything still
empty after that is a CF block or a malformed sog.

## Unmapped — no ZAP sog at all

These rely on free-text search. Either find a working sog or accept
slower / less-relevant results.

| Leaf | Sub-section |
|---|---|
| Samsung Galaxy Tab | טאבלטים — falls through to `c-tabletpc` via "טאבלט" generic. OK. |
| Lenovo Tab | same |
| ספקי כוח | חומרה — no sog. Try mapping to a generic PC-parts sog. |
| מעמדים לסלולר | תקשורת — no sog. |
| מזל"טים | צילום — no clean ZAP sog for drones. |
| מכשירי לחות | חימום — could map to `b-airrefresher` (air purifiers) — same sog. |

## Dropped from tree

For the record — these were removed in this pass:

- **Top-level: אופניים ואביזרים** — entire category. ZAP bicycle catalogue
  is 516 brand-only stubs out of 622. Needs specialty bike-retailer
  integration to be useful.
- **Top-level: רכב ואביזרים** — entire category. ZAP automotive coverage
  is sparse.
- **בית וגן → גינון חשמלי** — sub. Niche, low buy-frequency.
- **קונסולות משחק → משחקי PS5 / משחקי Nintendo** — items. ZAP doesn't
  cover game *titles* as a category.
- **צילום → מזחלות** — item. Wrong category (sleds/luggage in a photo
  sub).
- **פנאי וספורט → ספסלי כושר, מכשירי כפיפות ישיבה** — items. Not
  electric / electronic products.
